name: Problem Project
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Dependency installation
        run: npm install
      - name: Run tests
        run: npm test